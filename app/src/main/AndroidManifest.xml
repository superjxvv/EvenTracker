<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.testingv1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".addEvent" />
        <activity android:name=".EventsToday" />
        <activity android:name=".EditEvent" />
        <activity android:name=".EmailFeedBack" />
        <activity android:name=".GroupList" />
        <activity android:name=".groupCalendar" />
        <activity android:name=".addGroup" />
        <activity android:name=".FindUsers" />
        <activity android:name=".UserInfo" />
        <activity android:name=".RequestList" />
        <activity android:name=".EventsTodayGroup" />
        <activity android:name=".groupDetails"></activity>
    </application>

</manifest>